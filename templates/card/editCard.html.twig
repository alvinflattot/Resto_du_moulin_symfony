{% extends 'base.html.twig' %}

{% block title %}Modification de menu{% endblock %}

{% block body %}

    {# Entête #}
    <section class="row head-title">
        <div class="col-12  text-center head-title-overlay py-4">
            <h1>Modification du menu</h1>
        </div>
    </section>

    {# Section menu #}
    <section class="row  py-4 py-md-5 text-center  ">

        <div  class="col-12">
        
            {# barre de navigation de la carte du restaurant #}
            <nav class="row cards-nav mb-4  ">

                <ul class="d-flex justify-content-center list-group col-10 offset-1 list-group-horizontal-md p-0">
                    <li class="mb-2 list-group-item" id="menu-du-jour" data-type="menu-du-jour">
                        <a href="{{ path('card_type',{type: 'menu-du-jour'}) }}">Menu du jour</a>
                    </li>
                    <li class="mb-2 list-group-item" id="entree" data-type="entree">
                        <a href="{{ path('card_type',{type: 'entree'}) }}">Entrées</a>
                    </li>
                    <li class="mb-2 list-group-item" id="plat" data-type="plat">
                        <a href="{{ path('card_type',{type: 'plat'}) }}">Plats</a>
                    </li>
                    <li class="mb-2 list-group-item" id="dessert" data-type="dessert">
                        <a href="{{ path('card_type',{type: 'dessert'}) }}">Desserts</a>
                    </li>
                    <li class="mb-2 list-group-item" id="petit-chef" data-type="petit-chef">
                        <a href="{{ path('card_type',{type: 'petit-chef'}) }}"> Petit chef</a>
                    </li>
                    <li class="mb-2 list-group-item" id="emporter" data-type="emporter">
                        <a href="{{ path('card_type',{type: 'emporter'}) }}">À emporter</a>
                    </li>
                </ul>

            </nav>

            {# affichage de la carte  #}
            <div class="row ">

                <div class="col-10 offset-1 col-md-6 offset-md-3 border  rounded card-view  py-3  ">
                    
                    {# L'attribut action du formulaire doit être généré car ce dernier contient l'id de la page à modifier #}
                    {{ form_start(form, {attr: {class: 'row'}}, {attr: {'action' : path('card_edit', { id: form.vars.value.id })}}) }}

                        <div class="offset-2 col-8">
                            {# Champ titre #}
                            {{ form_row(form.title) }}
                        </div>

                        <div class="offset-2 col-8">
                            {# Champ contenu #}
                            {{ form_row(form.content, {attr: {rows: '12'}}) }}
                        </div>

                        <div class="offset-2 col-3 contact-btn-div">
                            {# Champ titre #}
                            {{ form_row(form.send) }}
                        </div>

                    {# Fermeture du formulaire #}
                    {{ form_end(form) }}
            
                    
                </div>
            
            </div>

        </div>
                
    </section>

    

{% endblock %}